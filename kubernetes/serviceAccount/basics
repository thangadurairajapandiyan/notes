1. By default one Sevice account named "default" will be created in default namespace. And If we craete the pod in default namespace and if we didnt specify theservice account
name in the yaml file, while creating the pod kubernetes will assign the "default" service account to this pod

2. If we create a new namespace, by default it wont have the "default" service account

